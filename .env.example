# Google Apps Script web app URL that handles the Partner enquiry form.
# See scripts/enquiry-email.gs for setup steps. Once deployed, paste the
# "Web app URL" here (and add the same value to Vercel project env vars).
NEXT_PUBLIC_ENQUIRY_ENDPOINT=https://script.google.com/macros/s/REPLACE_ME/exec
